version: "3"

services:

  app:
    build:
      context: ./
      dockerfile: Dockerfile
    env_file:
      - aws.env
    volumes:
      - ./:/app
    working_dir: /app
    command: bash -c "codeship/build.sh"
